- test fswatch handling on linux
- add unit tests
- fswatch issues (Git issues checken):
  - --directories not correctly working on FreeBSD (does not detect touch for plain files)
  - batches not correctly working on FreeBSD (creates one batch for one change if for large latencies)
  - creates 2 events for every change on cygwin
  - check https://github.com/emcrisostomo/fswatch/issues/151 (Excluded items are recursively scanned nonetheless)
